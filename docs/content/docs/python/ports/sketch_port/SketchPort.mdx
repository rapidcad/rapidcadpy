---
title: SketchPort
---

## Functions

<PyFunction name={"draw_line"} type={"(self, start_point, end_point)"}>

Draws a line from start_point to end_point.

Args:
    start_point (tuple): The starting point of the line (x, y).
    end_point (tuple): The ending point of the line (x, y).

<PySourceCode >

```python
@abstractmethod
def draw_line(self, start_point, end_point):
    """
    Draws a line from start_point to end_point.

    Args:
        start_point (tuple): The starting point of the line (x, y).
        end_point (tuple): The ending point of the line (x, y).
    """
    pass
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"start_point"} type={null} value={null} />
<PyParameter name={"end_point"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"draw_circle"} type={"(self, center, radius)"}>

<PySourceCode >

```python
@abstractmethod
def draw_circle(self, center, radius):
    pass
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"center"} type={null} value={null} />
<PyParameter name={"radius"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"draw_arc"} type={"(self, start_point, mid_point, end_point)"}>

<PySourceCode >

```python
@abstractmethod
def draw_arc(self, start_point, mid_point, end_point):
    pass
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"start_point"} type={null} value={null} />
<PyParameter name={"mid_point"} type={null} value={null} />
<PyParameter name={"end_point"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>