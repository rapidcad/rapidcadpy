---
title: Extrude
---

## Attributes

<PyAttribute name={"extent_one"} type={"float"} value={null} />

<PyAttribute name={"extent_two"} type={"Optional[float]"} value={"0.0"} />

<PyAttribute name={"extent_type"} type={"str"} value={"'OneSideFeatureExtentType'"} />

<PyAttribute name={"operation"} type={"str"} value={"'NewBodyFeatureOperation'"} />

<PyAttribute name={"direction"} type={"int"} value={"1"} />

<PyAttribute name={"symmetric"} type={"Optional[bool]"} value={"False"} />

<PyAttribute name={"taper_angle_one"} type={"Optional[float]"} value={"0.0"} />

<PyAttribute name={"taper_angle_two"} type={"Optional[float]"} value={"0.0"} />

<PyAttribute name={"name"} type={"Optional[str]"} value={"None"} />

## Functions

<PyFunction name={"__post_init__"} type={"(self)"}>

<PySourceCode >

```python
def __post_init__(self):
    self.extent_one = round(self.extent_one, 6)
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"to_json"} type={"(self) -> Dict[str, Any]"}>

<PySourceCode >

```python
def to_json(self) -> Dict[str, Any]:
    return {
        "extent_one": self.extent_one,
        "extent_two": self.extent_two,
        "symmetric": self.symmetric,
        "taper_angle_one": self.taper_angle_one,
        "taper_angle_two": self.taper_angle_two,
        "extent_type": self.extent_type,
        "operation": self.operation,
    }
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={"typing.Dict[str, typing.Any]"} />

</PyFunction>

<PyFunction name={"to_tensor"} type={"(self)"}>

<PySourceCode >

```python
def to_tensor(self):
    # Define constants locally since we can't import from models
    EXTENT_TYPE = [
        "OneSideFeatureExtentType",
        "TwoSidesFeatureExtentType",
        "SymmetricFeatureExtentType",
    ]
    EXTRUDE_OPERATIONS = [
        "NewBodyFeatureOperation",
        "JoinFeatureOperation",
        "CutFeatureOperation",
        "IntersectFeatureOperation",
    ]

    return torch.tensor(
        [
            float(self.extent_one),
            float(self.extent_two),
            float(self.symmetric),
            float(self.taper_angle_one),
            float(self.taper_angle_two),
            EXTENT_TYPE.index(self.extent_type),
            EXTRUDE_OPERATIONS.index(self.operation),
        ],
        dtype=torch.float,
    )
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"from_tensor"} type={"(cls, tensor) -> Extrude"}>

<PySourceCode >

```python
@classmethod
def from_tensor(cls, tensor) -> "Extrude":
    # Define constants locally since we can't import from models
    EXTENT_TYPE = [
        "OneSideFeatureExtentType",
        "TwoSidesFeatureExtentType",
        "SymmetricFeatureExtentType",
    ]
    EXTRUDE_OPERATIONS = [
        "NewBodyFeatureOperation",
        "JoinFeatureOperation",
        "CutFeatureOperation",
        "IntersectFeatureOperation",
    ]

    return cls(
        extent_one=float(tensor[0]),
        extent_two=float(tensor[1]),
        symmetric=bool(tensor[2]),
        taper_angle_one=float(tensor[3]),
        taper_angle_two=float(tensor[4]),
        extent_type=EXTENT_TYPE[int(tensor[5])],
        operation=EXTRUDE_OPERATIONS[int(tensor[6])],
    )
```

</PySourceCode>

<div >

<PyParameter name={"cls"} type={null} value={null} />
<PyParameter name={"tensor"} type={null} value={null} />

</div>

<PyFunctionReturn type={"rapidcadpy.extrude.Extrude"} />

</PyFunction>

<PyFunction name={"from_json"} type={"(json)"}>

<PySourceCode >

```python
@staticmethod
def from_json(json):
    return Extrude(
        extent_one=json["extent_one"],
        extent_two=json["extent_two"],
        symmetric=json["symmetric"],
        taper_angle_one=json["taper_angle_one"],
        taper_angle_two=json["taper_angle_two"],
        extent_type=json["extent_type"],
        operation=json["operation"],
    )
```

</PySourceCode>

<div >

<PyParameter name={"json"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"to_python"} type={"(self, index=0)"}>

<PySourceCode >

```python
def to_python(self, index: Optional[int] = 0):
    return f"Extrude(extent_one={self.extent_one}, extent_two={self.extent_two}, symmetric={self.symmetric}, taper_angle_one={self.taper_angle_one}, taper_angle_two={self.taper_angle_two})"
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"index"} type={"Optional[int]"} value={"0"} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"__eq__"} type={"(self, other)"}>

<PySourceCode >

```python
def __eq__(self, other):
    return (
        self.extent_one == other.extent_one
        and self.extent_two == other.extent_two
        and self.symmetric == other.symmetric
        and self.taper_angle_one == other.taper_angle_one
        and self.taper_angle_two == other.taper_angle_two
    )
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"other"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"transform"} type={"(self, scale)"}>

<PySourceCode >

```python
def transform(self, scale):
    self.extent_one *= scale
    self.extent_two *= scale
    return self
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"scale"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"numericalize"} type={"(self, n=256)"}>

<PySourceCode >

```python
def numericalize(self, n=256):
    n -= 1
    self.extent_one = round(self.extent_one * n)
    self.extent_two = round(self.extent_two * n)
    self.taper_angle_one = round(self.taper_angle_one * n)
    self.taper_angle_two = round(self.taper_angle_two * n)
    return self
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"n"} type={null} value={"256"} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"denumericalize"} type={"(self, n)"}>

<PySourceCode >

```python
def denumericalize(self, n):
    self.extent_one = self.extent_one / n
    self.extent_two = self.extent_two / n
    self.taper_angle_one = self.taper_angle_one / n
    self.taper_angle_two = self.taper_angle_two / n
    return self
```

</PySourceCode>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"n"} type={null} value={null} />

</div>

<PyFunctionReturn type={null} />

</PyFunction>

<PyFunction name={"__init__"} type={"(self, extent_one, extent_two=0.0, extent_type='OneSideFeatureExtentType', operation='NewBodyFeatureOperation', direction=1, symmetric=False, taper_angle_one=0.0, taper_angle_two=0.0, name=None) -> None"}>

<div >

<PyParameter name={"self"} type={null} value={null} />
<PyParameter name={"extent_one"} type={"float"} value={null} />
<PyParameter name={"extent_two"} type={"Optional[float]"} value={"0.0"} />
<PyParameter name={"extent_type"} type={"str"} value={"'OneSideFeatureExtentType'"} />
<PyParameter name={"operation"} type={"str"} value={"'NewBodyFeatureOperation'"} />
<PyParameter name={"direction"} type={"int"} value={"1"} />
<PyParameter name={"symmetric"} type={"Optional[bool]"} value={"False"} />
<PyParameter name={"taper_angle_one"} type={"Optional[float]"} value={"0.0"} />
<PyParameter name={"taper_angle_two"} type={"Optional[float]"} value={"0.0"} />
<PyParameter name={"name"} type={"Optional[str]"} value={"None"} />

</div>

<PyFunctionReturn type={"None"} />

</PyFunction>