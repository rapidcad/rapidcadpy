repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version - update to latest stable
    rev: v0.8.4
    hooks:
      # Run the linter with auto-fix
      - id: ruff
        args: [--fix, --ignore, F841]  # F841 = unused variable
